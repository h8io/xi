name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  release:
    secrets: inherit
    uses: h8io/gha/.github/workflows/release.yaml@v6
    with:
      java-version: 17
      publish-pages: true
