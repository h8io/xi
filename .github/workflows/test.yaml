name: Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    secrets: inherit
    uses: h8io/gha/.github/workflows/test.yaml@v4.0.0-M2
    with:
      timeout-minutes: 5
